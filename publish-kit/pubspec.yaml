name: publish_kit
description: Release driver for the wxscan crates on crates.io and packages on pub.dev.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  args: ^2.7.0
  path: ^1.9.1
  yaml: ^3.1.3
  yaml_edit: ^2.2.2

dev_dependencies:
  lints: ^6.0.0
